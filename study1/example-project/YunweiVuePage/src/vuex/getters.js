//test
export const getCount = state => {
    return state.count
}


export const token = (state) => {
    return state.token
}

export const expiresAt = (state) => {
    return state.expiresAt
}

export const asyncRouters = (state) => {
    return state.asyncRouters
}